#include <bits/stdc++.h>
#include "DBSCAN.h"
#include "Cluster.h"
#include "Relation.h"
#include "Subspace.h"
#include "SUBCLU.h"
using namespace std;

//run with g++ -o test BitsUtil.cpp DBSCAN.cpp Cluster.cpp Subspace.cpp Relation.cpp SUBCLU.cpp ReadInput.cpp testing.cpp
int main()

{
    // SUBCLU subclu("input.txt", 2, 20, 1);
    // map<Subspace, vector<Cluster>> result = subclu.run();

    // for (auto i : result)
    // {
    //     Subspace obj = i.first;
    //     obj.print();
    //     for (auto c : i.second)
    //     {
    //         c.print();
    //     }
    // }
}